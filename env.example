# =============================================================================
# Minepanel Environment Configuration
# =============================================================================
# Copy this file to .env and configure your values
# cp env.example .env
# =============================================================================

# -----------------------------------------------------------------------------
# Authentication (Required)
# -----------------------------------------------------------------------------
JWT_SECRET=                                    # Generate with: openssl rand -base64 32
CLIENT_USERNAME=admin
CLIENT_PASSWORD=admin

# -----------------------------------------------------------------------------
# Directories
# -----------------------------------------------------------------------------
BASE_DIR=$PWD                                  # Base directory on host (required for Docker socket)

# -----------------------------------------------------------------------------
# URLs (Important for remote access and CORS)
# -----------------------------------------------------------------------------
FRONTEND_URL=http://localhost:3000            # Must match browser URL (controls CORS)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8091
NEXT_PUBLIC_FILEBROWSER_URL=http://localhost:8080

# -----------------------------------------------------------------------------
# Language
# -----------------------------------------------------------------------------
NEXT_PUBLIC_DEFAULT_LANGUAGE=en               # Options: en, es, nl

# -----------------------------------------------------------------------------
# Network (Optional - for LAN play)
# -----------------------------------------------------------------------------
# HOST_LAN_IP=                                # Your LAN IP for local network play
                                              # Get it with:
                                              #   Mac: ipconfig getifaddr en0
                                              #   Linux: hostname -I | awk '{print $1}'
                                              #   Windows: (Get-NetIPAddress -AddressFamily IPv4 -InterfaceAlias "Ethernet").IPAddress

# =============================================================================
# Configuration Examples
# =============================================================================

# Local access (default):
#   FRONTEND_URL=http://localhost:3000
#   NEXT_PUBLIC_BACKEND_URL=http://localhost:8091
#   NEXT_PUBLIC_FILEBROWSER_URL=http://localhost:8080

# Remote access (by IP):
#   FRONTEND_URL=http://192.168.1.100:3000
#   NEXT_PUBLIC_BACKEND_URL=http://192.168.1.100:8091
#   NEXT_PUBLIC_FILEBROWSER_URL=http://192.168.1.100:8080

# With domain and SSL:
#   FRONTEND_URL=https://minepanel.yourdomain.com
#   NEXT_PUBLIC_BACKEND_URL=https://api.yourdomain.com
#   NEXT_PUBLIC_FILEBROWSER_URL=https://files.yourdomain.com

# External drive:
#   BASE_DIR=/mnt/external-drive/minepanel

# LAN IP for local network play:
#   HOST_LAN_IP=192.168.1.100

