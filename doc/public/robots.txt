# Minepanel Documentation - Robots Configuration
# Allow all compliant crawlers
# /AGENTS is excluded via meta noindex (must stay crawlable so bots can see it)

User-agent: *
Allow: /
Disallow: /hashmap.json

# Note: Reputable AI crawlers (e.g. GPTBot, Anthropic) are allowed
# to index the docs to improve discoverability and integrations.
# If abusive scraping appears, re-add specific Disallow entries.

# Sitemap
Sitemap: https://minepanel.ketbome.com/sitemap.xml
