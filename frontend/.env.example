NEXT_PUBLIC_BACKEND_URL='http://localhost:8091' # URL of the backend API, it's important start with http:// or https://
NEXT_PUBLIC_BASE_PATH='' # Optional frontend subpath, e.g. /minepanel. Requires rebuild
NEXT_PUBLIC_DEFAULT_LANGUAGE=en # es, nl or en
